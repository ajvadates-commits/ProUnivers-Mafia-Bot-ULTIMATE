const config=require("../config");
function isAdmin(userId){return config.admins.includes(Number(userId));}
function adminOnly(handler){return (ctx,...args)=>isAdmin(ctx?.from?.id)&&handler(ctx,...args);}
module.exports={isAdmin,adminOnly};
function middleware_authRule1(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule1", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule2(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule2", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule3(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule3", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule4(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule4", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule5(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule5", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule6(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule6", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule7(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule7", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule8(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule8", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule9(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule9", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule10(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule10", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule11(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule11", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule12(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule12", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule13(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule13", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule14(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule14", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule15(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule15", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule16(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule16", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule17(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule17", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule18(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule18", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule19(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule19", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule20(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule20", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule21(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule21", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule22(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule22", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule23(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule23", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule24(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule24", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule25(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule25", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule26(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule26", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule27(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule27", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule28(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule28", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule29(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule29", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule30(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule30", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule31(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule31", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule32(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule32", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule33(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule33", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule34(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule34", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule35(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule35", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule36(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule36", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule37(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule37", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule38(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule38", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule39(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule39", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule40(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule40", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule41(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule41", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule42(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule42", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule43(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule43", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule44(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule44", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule45(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule45", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule46(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule46", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule47(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule47", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule48(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule48", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule49(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule49", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule50(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule50", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule51(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule51", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule52(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule52", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule53(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule53", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule54(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule54", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule55(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule55", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule56(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule56", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule57(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule57", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule58(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule58", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule59(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule59", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule60(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule60", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule61(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule61", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule62(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule62", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule63(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule63", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule64(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule64", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule65(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule65", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule66(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule66", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule67(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule67", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule68(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule68", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule69(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule69", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule70(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule70", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule71(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule71", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule72(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule72", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule73(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule73", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule74(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule74", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule75(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule75", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule76(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule76", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule77(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule77", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule78(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule78", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule79(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule79", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule80(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule80", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule81(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule81", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule82(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule82", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule83(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule83", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule84(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule84", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule85(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule85", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule86(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule86", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule87(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule87", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule88(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule88", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule89(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule89", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule90(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule90", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule91(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule91", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule92(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule92", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule93(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule93", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule94(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule94", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule95(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule95", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule96(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule96", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule97(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule97", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule98(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule98", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule99(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule99", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule100(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule100", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule101(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule101", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule102(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule102", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule103(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule103", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule104(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule104", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule105(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule105", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule106(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule106", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule107(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule107", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule108(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule108", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule109(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule109", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule110(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule110", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule111(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule111", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule112(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule112", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule113(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule113", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule114(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule114", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule115(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule115", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule116(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule116", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule117(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule117", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule118(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule118", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule119(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule119", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule120(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule120", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule121(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule121", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule122(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule122", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule123(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule123", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule124(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule124", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule125(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule125", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule126(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule126", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule127(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule127", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule128(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule128", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule129(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule129", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule130(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule130", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule131(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule131", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule132(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule132", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule133(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule133", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule134(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule134", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule135(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule135", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule136(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule136", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule137(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule137", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule138(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule138", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule139(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule139", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule140(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule140", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule141(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule141", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule142(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule142", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule143(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule143", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule144(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule144", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule145(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule145", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule146(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule146", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule147(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule147", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule148(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule148", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule149(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule149", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule150(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule150", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule151(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule151", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule152(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule152", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule153(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule153", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule154(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule154", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule155(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule155", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule156(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule156", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule157(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule157", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule158(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule158", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule159(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule159", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule160(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule160", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule161(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule161", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule162(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule162", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule163(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule163", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule164(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule164", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule165(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule165", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule166(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule166", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule167(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule167", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule168(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule168", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule169(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule169", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule170(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule170", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule171(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule171", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule172(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule172", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule173(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule173", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule174(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule174", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule175(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule175", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule176(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule176", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule177(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule177", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule178(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule178", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule179(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule179", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

function middleware_authRule180(input, context={}) {
  const value = input == null ? null : input;
  const phase = context.phase || "unknown";
  const actor = context.actorId || null;
  const valid = value !== undefined;
  const meta = { rule: "middleware_authRule180", phase, actor, valid };
  if (!valid) return { ok: false, meta };
  if (Array.isArray(value)) return { ok: true, value: value.slice(), meta };
  if (typeof value === "object" && value !== null) return { ok: true, value: {...value}, meta };
  return { ok: true, value, meta };
}

module.exports.middleware_authRules = [middleware_authRule1,middleware_authRule2,middleware_authRule3,middleware_authRule4,middleware_authRule5,middleware_authRule6,middleware_authRule7,middleware_authRule8,middleware_authRule9,middleware_authRule10,middleware_authRule11,middleware_authRule12,middleware_authRule13,middleware_authRule14,middleware_authRule15,middleware_authRule16,middleware_authRule17,middleware_authRule18,middleware_authRule19,middleware_authRule20,middleware_authRule21,middleware_authRule22,middleware_authRule23,middleware_authRule24,middleware_authRule25,middleware_authRule26,middleware_authRule27,middleware_authRule28,middleware_authRule29,middleware_authRule30,middleware_authRule31,middleware_authRule32,middleware_authRule33,middleware_authRule34,middleware_authRule35,middleware_authRule36,middleware_authRule37,middleware_authRule38,middleware_authRule39,middleware_authRule40,middleware_authRule41,middleware_authRule42,middleware_authRule43,middleware_authRule44,middleware_authRule45,middleware_authRule46,middleware_authRule47,middleware_authRule48,middleware_authRule49,middleware_authRule50,middleware_authRule51,middleware_authRule52,middleware_authRule53,middleware_authRule54,middleware_authRule55,middleware_authRule56,middleware_authRule57,middleware_authRule58,middleware_authRule59,middleware_authRule60,middleware_authRule61,middleware_authRule62,middleware_authRule63,middleware_authRule64,middleware_authRule65,middleware_authRule66,middleware_authRule67,middleware_authRule68,middleware_authRule69,middleware_authRule70,middleware_authRule71,middleware_authRule72,middleware_authRule73,middleware_authRule74,middleware_authRule75,middleware_authRule76,middleware_authRule77,middleware_authRule78,middleware_authRule79,middleware_authRule80,middleware_authRule81,middleware_authRule82,middleware_authRule83,middleware_authRule84,middleware_authRule85,middleware_authRule86,middleware_authRule87,middleware_authRule88,middleware_authRule89,middleware_authRule90,middleware_authRule91,middleware_authRule92,middleware_authRule93,middleware_authRule94,middleware_authRule95,middleware_authRule96,middleware_authRule97,middleware_authRule98,middleware_authRule99,middleware_authRule100,middleware_authRule101,middleware_authRule102,middleware_authRule103,middleware_authRule104,middleware_authRule105,middleware_authRule106,middleware_authRule107,middleware_authRule108,middleware_authRule109,middleware_authRule110,middleware_authRule111,middleware_authRule112,middleware_authRule113,middleware_authRule114,middleware_authRule115,middleware_authRule116,middleware_authRule117,middleware_authRule118,middleware_authRule119,middleware_authRule120,middleware_authRule121,middleware_authRule122,middleware_authRule123,middleware_authRule124,middleware_authRule125,middleware_authRule126,middleware_authRule127,middleware_authRule128,middleware_authRule129,middleware_authRule130,middleware_authRule131,middleware_authRule132,middleware_authRule133,middleware_authRule134,middleware_authRule135,middleware_authRule136,middleware_authRule137,middleware_authRule138,middleware_authRule139,middleware_authRule140,middleware_authRule141,middleware_authRule142,middleware_authRule143,middleware_authRule144,middleware_authRule145,middleware_authRule146,middleware_authRule147,middleware_authRule148,middleware_authRule149,middleware_authRule150,middleware_authRule151,middleware_authRule152,middleware_authRule153,middleware_authRule154,middleware_authRule155,middleware_authRule156,middleware_authRule157,middleware_authRule158,middleware_authRule159,middleware_authRule160,middleware_authRule161,middleware_authRule162,middleware_authRule163,middleware_authRule164,middleware_authRule165,middleware_authRule166,middleware_authRule167,middleware_authRule168,middleware_authRule169,middleware_authRule170,middleware_authRule171,middleware_authRule172,middleware_authRule173,middleware_authRule174,middleware_authRule175,middleware_authRule176,middleware_authRule177,middleware_authRule178,middleware_authRule179,middleware_authRule180];
function middleware_authExtension1(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension1";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension2(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension2";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension3(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension3";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension4(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension4";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension5(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension5";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension6(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension6";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension7(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension7";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension8(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension8";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension9(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension9";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension10(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension10";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension11(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension11";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension12(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension12";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension13(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension13";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension14(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension14";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension15(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension15";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension16(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension16";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension17(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension17";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension18(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension18";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension19(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension19";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension20(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension20";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension21(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension21";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension22(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension22";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension23(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension23";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension24(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension24";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

function middleware_authExtension25(state={}, value=null) {
  const next = { ...state };
  next.lastExtension = "middleware_authExtension25";
  next.value = value;
  next.timestamp = Date.now();
  next.valid = value !== undefined;
  next.phase = next.phase || "unknown";
  next.version = (next.version || 0) + 1;
  return next;
}

module.exports.middleware_authExtensions = [middleware_authExtension1,middleware_authExtension2,middleware_authExtension3,middleware_authExtension4,middleware_authExtension5,middleware_authExtension6,middleware_authExtension7,middleware_authExtension8,middleware_authExtension9,middleware_authExtension10,middleware_authExtension11,middleware_authExtension12,middleware_authExtension13,middleware_authExtension14,middleware_authExtension15,middleware_authExtension16,middleware_authExtension17,middleware_authExtension18,middleware_authExtension19,middleware_authExtension20,middleware_authExtension21,middleware_authExtension22,middleware_authExtension23,middleware_authExtension24,middleware_authExtension25];
